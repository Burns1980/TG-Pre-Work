/*
Inheritance is a way for developers to use less code by creating a superclass that possesses
common properties and methods of subclasses. These subclasses then inherit the characteristics
of the parent class when created. From there, these subclasses add their own custom characteristics.
*/
