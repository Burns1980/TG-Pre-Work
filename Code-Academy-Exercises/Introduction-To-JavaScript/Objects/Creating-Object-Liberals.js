// Write your fasterShip object literal below
let fasterShip = {
  color: 'silver',
  'Fuel Type': 'Turbo Fuel'
};
/*
Objects are filled with unordered data which is organized in a key-value pairing.
The key is the identifier that points to a location in memory that holds a value.
A key's value can be any data type. Each kv pair is separated with a comma. Keys
are strings but quotes can be omitted if the key has no special characters.
*/
