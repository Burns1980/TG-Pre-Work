const entree = 'Enchiladas';
console.log(entree);
entree = 'Tacos'

// The const variable cannot be changed once it is declared. Also, upon declaration, it
// must be initialized with a value or else you'll get a syntax error. Lastly, trying to
// change a const variable will result in a TypeError.
