var favoriteFood = 'pizza';
var numOfSlices = 8;
console.log(favoriteFood);
console.log(numOfSlices);

//camel casing is when multiple words are grouped into one word with the first word lowercase and the remaining words uppercase.
// variables cannot start with numbers, are case sensitive and cannot be kws.
