let changeMe = true;
changeMe = false;
console.log(changeMe);

//The let keyword can be reassigned a different value. If declared without assigning
//it a value, it automatically gets the undefined value.
