const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('run errands', 'organize climbing gear');
console.log(chores);

//The push method adds elements to the end of the array, which mutates an array.
//The push method is also called a destructive array method becasue it changes the initial array.
