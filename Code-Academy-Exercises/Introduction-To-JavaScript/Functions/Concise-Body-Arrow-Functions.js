const plantNeedsWater = day => day === 'Wednesday' ? true : false;

// The most condensed form of a function is called concised body.
// functions that take only 1 Parameter don't need () around the Parameter. functions that have none or more than one do.
// A function body that has only a single line statement does not need {}. These types of functions have an implicit return
// as the return kw is not used. ex. const funName = param1 => 3 * 2;
