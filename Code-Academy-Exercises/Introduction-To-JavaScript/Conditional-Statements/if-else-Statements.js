let sale = true;

sale = false;

if(sale) {
  console.log('Time to buy!');
}

else {
  console.log('Time to wait for a sale.');
}

// else can be added to an if else block to add a default behavior if all conditions fail.
// if...else statements allow us to automate binary decisions.
