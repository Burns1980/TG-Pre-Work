console.log('Teaching the world how to code'.length);
// one property of a string is the length property. To use it, append the dot operator
// (.) and length to the end of the string to get it's length.
